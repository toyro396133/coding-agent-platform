import { LandingPage } from '@/components/landing-page'

export const metadata = {
  title: 'Freebuff — Six Agents, One Prompt',
  description: 'Every major CLI coding agent running in parallel sandboxes. Compare results. Pick the best. Free.',
}

export default function Landing() {
  return <LandingPage />
}
