# מפת דרכים ליישום חזון סוכן הקוד והפלטפורמה (Implementation Roadmap)

## מבוא והקשר (Context)

מסמך זה מהווה את תוכנית העבודה האופרטיבית (Roadmap) המפורטת ביותר להפיכת הפלטפורמה הנוכחית למערכת State of the Art המשלבת:
1. נתב מודלים חכם (Model Router).
2. נתב API חיצוני (API Router).
3. סוכן ענן עצמאי (Native Cloud Agent).
4. מפעיל סוכנים (Orchestrator).
5. בוחן חזותי (Visual Web Tester).

התוכנית מחולקת ל-5 שלבים מרכזיים (Phases), כאשר כל שלב מפורק למשימות, תתי-משימות, דרישות ארכיטקטוניות והסברים נרחבים. המטרה היא להעניק למערכת יכולות המקבילות לאלו של Devin, Cursor, Aider ו-Kimi, אך מותאמות לתשתית מבוססת-ענן וארגזי חול (Sandboxes) אובטחים.

---

## שלב 1: ארכיטקטורת נתב מודלים חכם (Smart Model Router)

**המטרה:** בניית שכבת ניתוב שתדע לנתח פרומפטים בזמן אמת ולהפנות אותם למודל השפה (LLM) המתאים ביותר. היעד הוא להפחית את עלויות ה-API בעד 70% בהשוואה ל-baseline של שימוש בלעדי במודלי עילית (כגון `claude-3.5-sonnet`), ולשפר זמני תגובה למשימות פשוטות. המדידה תתבצע על בסיס תמהיל משימות צפוי של 60% פשוטות (Rules 1-2) ו-40% מורכבות, ותבחן את היחס בין איכות הפלט, זמן העיבוד והעלות פר טוקן.

### 1.1 ניתוח מורכבות בקשה (Prompt Complexity Analysis) - ✅ הושלם

*   **הקשר:** לפני שמריצים בקשה דרך מודל יקר ואיטי כמו `claude-3.5-sonnet`, יש להבין האם זו בקשה פשוטה (למשל הוספת הערות, עדכון גרסה ב-package.json) או מורכבת (בניית קומפוננטה מאפס, ארכיטקטורה חדשה).
*   **תתי-שלבים:**
    1.  הגדרת מנגנון Classifier (סיווג) ראשוני לבקשות המשתמש עם AI SDK.
    2.  שילוב ספריות ראוטינג קיימות (כמו RouteLLM) או שימוש במודל מהיר וזול (כמו `gpt-4o-mini` או `claude-3-haiku`) כשכבת proxy רק כדי לסווג את הבקשה.
    3.  יצירת סקלת מורכבות (מ-1 עד 5) לכל משימה חדשה.
    4.  הגדרת מנוע חוקים (Rules Engine): משימות ברמת 1-2 יופנו למודלים מהירים/זולים, משימות 3-5 יופנו למודלי עילית.

### 1.2 תמיכה ב-Fallbacks ומנגנוני שרידות - ✅ הושלם

*   **הקשר:** רשתות API של מודלים סובלות לעיתים מעומסים, קריסות (Downtimes) או שגיאות 5xx. המערכת חייבת לשמור על רציפות תפקודית.
*   **תתי-שלבים:**
    1.  מימוש מנגנון Retry אוטומטי עם Exponential Backoff בתוך הקריאות של ה-AI SDK Core.
    2.  הגדרת רשימת מודלים חלופיים (Fallback Models). למשל, אם מודל `o1` נכשל, נסה `claude-3.5-sonnet`.
    3.  שימוש מתקדם ב-Vercel AI Gateway (שכבר מובנה בפרויקט) לניהול הגדרות ה-Fallback בצורה מרוכזת ונקייה.

### 1.3 Load Balancing וניהול תעבורה (Rate Limits) - ✅ הושלם (באמצעות AI Gateway)

*   **הקשר:** בריבוי בקשות משתמשים בו-זמנית, הפלטפורמה עלולה להיתקל בחסימות (429 Too Many Requests) מצד ספקי ה-AI.
*   **תתי-שלבים:**
    1.  מעקב צמוד אחר מגבלות הקצב של ספקי ה-API דרך Headers של ה-Response ושמירתם בסטייט זמני (למשל ב-Redis או בטבלה רזה ב-Neon).
    2.  יישום מנגנון API Key Rotation למקרה של חריגות ממכסת ה-Rate Limit (חלוקת העומס על פני מספר מפתחות/חשבונות).
    3.  יצירת מנגנון השהייה (Throttling / Queuing) לבקשות רקע שאינן דורשות זמן תגובה מיידי (למשל אינדוקס RAG) כדי להבטיח זמינות לצרכים קריטיים.

---

## שלב 2: בניית שכבת API חיצונית (API Router)

**המטרה:** חשיפת הסוכן ונתב המודלים לעולם החיצון. בכך הפלטפורמה מפסיקה להיות רק אפליקציית Web והופכת לתשתית "Agent-as-a-Service" שניתן לצרוך גם מ-CLI וגם מתוספי VSCode.

### 2.1 יצירת ממשק תואם לתקן OpenAI (OpenAI Compatible API)

*   **הקשר:** בניית API שיהיה כמעט Drop-in Replacement ל-OpenAI, כדי שכלים שונים יוכלו להתחבר לפלטפורמה שלנו ללא שינויי קוד משמעותיים אצלם.
*   **תתי-שלבים:**
    1.  הגדרת API Route חדש ב-Next.js (לדוגמה: `POST /api/agent/v1/chat/completions`).
    2.  עיבוד ופירוק הנתונים הנכנסים (Parsing payload) והמרתם לפורמט הנדרש להפעלת ה-Orchestrator שלנו.
    3.  הטמעת תמיכה מלאה בהחזרת מידע ברצף (Streaming) תוך שימוש ב-Server-Sent Events (SSE).

### 2.2 ניהול הרשאות וגישה (Authentication & Authorization)

*   **הקשר:** חובה לאבטח את השירות ולדעת לשייך בקשות API למשתמש ספציפי במערכת לצרכי חיוב ומעקב.
*   **תתי-שלבים:**
    1.  הוספת מסך "API Keys" בפרופיל המשתמש להנפקת מפתחות סודיים חדשים (למשל בפורמט `sk-platform-...`).
    2.  אחסון המפתחות באופן מאובטח (Hashed) במסד הנתונים Neon בטבלת `api_keys` ייעודית.
    3.  כתיבת Middleware (או שיפור הקיים) שיבדוק את ה-`Authorization: Bearer <token>` בכל קריאת API נכנסת ויוודא הרשאות.

### 2.3 סנכרון עם ארגז החול והחזרת Diffs אוטומטית

*   **הקשר:** בניגוד לצ'אט טקסטואלי רגיל, ה-API שלנו מריץ קוד. הלקוח (החיצוני) שואל שאלה ומצפה לקבל את השינויים שבוצעו בקוד.
*   **תתי-שלבים:**
    1.  בניית לוגיקה שמקימה Vercel Sandbox אסינכרונית מייד עם קבלת בקשת ה-API.
    2.  הפעלת הסוכן (Native Agent) בתוך ה-Sandbox ואיסוף התוצאות מהמסוף הווירטואלי.
    3.  הגדרת חוזה API חד-משמעי: תיעוד סכמות אירועי ה-SSE, מזהה העבודה (Job ID), וסטטוסי ההתקדמות (לדוגמה `started`, `processing`, `completed`, `failed`).
    4.  החזרת התוצרים: בניית פורמט JSON מחמיר להחזרת ה-Diff (מבנה Patch ברור), מבנה שגיאות מפורט (Error details & codes), תמיכה בביטול משימות (Cancellation), ותמיכה ב-Idempotency (למניעת ביצוע כפול של אותה בקשה) כך שלקוחות יוכלו לממש את הממשק ללא פרשנות.

---

## שלב 3: פיתוח סוכן ענן עצמאי (Native Cloud Agent)

**המטרה:** במקום להסתמך על כלי CLI מוכנים מראש שמותקנים בארגז החול (כמו Aider או Claude Code החשופים למגבלות שינוי קודם), נפתח ליבת סוכן טבעית הנשלטת במלואה ב-Next.js באמצעות `Vercel AI SDK Core`.

### 3.1 בניית Agent Loop מרכזי

*   **הקשר:** יצירת הלולאה האינסופית-למחצה (Agentic Loop) שמאפשרת לסוכן לעבוד, להפעיל כלים, לקרוא את התוצאות ולהחליט על צעדיו הבאים באופן אוטונומי.
*   **תתי-שלבים:**
    1.  הטמעת שימוש ישיר בפונקציה `generateText` של AI SDK Core, והזרקת פרומפט המערכת (System Prompt) העדכני.
    2.  יצירת State Machine מתקדם לניהול מחזור חיי המשימה: `Analyze` -> `Plan` -> `Execute` -> `Verify` -> `Done`.
    3.  אכיפת מגבלות משימה מפורשות מעבר ל-Retries: הגדרת תקציב (Budgets) למשימה הכולל מספר צעדים מקסימלי, מגבלת זמן, מגבלת טוקנים ועלות מירבית, וכן תמיכה במנגנון ביטול אקטיבי (Cancellation). במקרה של חריגה מאחד התקציבים או ביטול, יש לעצור את הלולאה במיידי ולהחזיר תוצאה חלקית או שגיאה מוגדרת.
    4.  ניהול חלון קונטקסט (Context Window Management): מנגנון לקיצוץ או תמצות היסטוריית השיחה כשהיא ארוכה מדי.

### 3.2 כלים מותאמים אישית (Custom Tools for Sandbox)

*   **הקשר:** הענקת יכולות מוטוריות רבות עוצמה לסוכן כדי שיוכל לטפל בקבצים ולהריץ פקודות בסביבת הענן בצורה חכמה (כמו ב-Cursor או ב-Aider).
*   **תתי-שלבים:**
    1.  פיתוח כלי `read_file_ast`: מעבר לקריאת טקסט פשוטה, כלי זה יקרא קובץ וינתח את ה-Abstract Syntax Tree שלו (ב-TypeScript) כדי להחזיר לסוכן תקציר של המבנה ללא צורך בהדפסת אלפי שורות (חוסך טוקנים).
    2.  פיתוח כלי `write_file_patch`: במקום כלי שמחליף את כל תוכן הקובץ (מסוכן לטעויות וצורך טוקנים רבים), פיתוח כלי שיודע לקבל בלוקי Hunk (כמו גיט) או פקודות חיפוש-והחלפה ולהחיל אותם על הקובץ.
    3.  פיתוח כלי `run_bash_with_timeout`: מעטפת מאובטחת לפקודות Shell ב-Sandbox הכוללת ניהול שגיאות, אולטימטום זמן-ריצה (Timeout), והגנות קפדניות: בידוד מערכת קבצים ותהליכים, חסימת גישה מוחלטת ל-Secrets/Environment Variables רגישים, Allowlist ליציאת רשת, מגבלות משאבים (CPU/RAM/Disk), הגבלת גודל פלט מירבי, והריגה נקייה של כל עץ התהליכים (Process Tree) כדי למנוע זליגת משאבים (לדוגמה, שרת שנתקע).

### 3.3 תיקון עצמי חי מול עורך קוד (LSP Integration & Self-Healing)

*   **הקשר:** הסוכן חייב לזהות טעויות שנוצרו (כמו שגיאות TypeScript) בטרם הוא מדווח שהמשימה הסתיימה.
*   **תתי-שלבים:**
    1.  הגדרת הפעלת שרת שפה ברקע (למשל `tsserver` או `eslint` דמון) בתוך ה-Vercel Sandbox עם עלייתו.
    2.  פיתוח כלי ייעודי שיופעל אוטומטית לאחר כל שינוי קוד, שתפקידו "לקצור" (Extract) את רשימת השגיאות והאזהרות (Diagnostics) מה-LSP.
    3.  הזנת רשימת השגיאות באופן אוטומטי חזרה לפרומפט בתוך ה-Agent Loop. המערכת תחזור על הלולאה (Self Healing) עד לקבלת קומפילציה תקינה, או עד להגעה למגבלת ניסיונות (Max Retries).

---

## שלב 4: מפעיל סוכנים ומנהל פרויקטים (Orchestrator & Project Manager)

**המטרה:** מדובר בהרחבה של מנגנון ה-Orchestrator הקיים (בהתאם לחוזה ה-capability packs: תמיכה במצבי basic/enhanced/auto ותאימות לאחור ללא תלות מלאה ב-sandbox בשלב הראשון), המעניקה זיכרון רחב, והכנסת ביקורת אנושית (Human-in-the-loop). חשוב להבהיר ש-Repo-wide RAG ו-semantic_code_search מיושמים במסגרת הרחבה זו ולא כרכיבים נפרדים, תוך הקפדה על סדר יישום המתואר במסמכים המקבילים.

### 4.1 אינדוקס והבנת הקשר עמוקה (Repo-wide RAG)

*   **הקשר:** כדי לתפקד כמו Cursor, יש ליישם RAG על מאגר הקוד, אך במסגרת מודול ה-Orchestrator.
*   **תתי-שלבים:**
    1.  לפני פיתוח האינדוקס, יש להגדיר מדיניות אבטחה מחמירה ל-Repo-wide RAG: החלוקת קבצי סודות (.env וכו') וקבצים אוטומטיים, ביצוע Redaction למידע רגיש, ובידוד מוחלט של ה-Embeddings לפי Repository ו-Tenant. יש לאכוף הרשאות Retrieval לפי בעלות המאגר, ולהקים מנגנון למחיקה ו-Versioning של אינדקסים ב-Neon/pgvector.
    2.  בניית מנוע צד-שרת הסורק את קבצי הפרויקט (המותרים), מפרק אותם ל-Chunks, ומייצר Embeddings.
    3.  שמירת ה-Embeddings ב-Neon תוך שימוש ב-`pgvector`.
    4.  הוספת כלי (Tool) מיוחד ל-Orchestrator: `semantic_code_search` (שיכבד את מדיניות ההרשאות).

### 4.2 תכנון היררכי בשילוב המשתמש (Human-in-the-loop Planning)

*   **הקשר:** הפעלת סוכנים אוטומטית על סמך פרומפט ראשוני עשויה לגרום לסוכן "לרוץ לכיוון הלא נכון". הפתרון: תכנון תחילה ואישור גרסאות מנוהל.
*   **תתי-שלבים:**
    1.  הוספת סטטוס מחזור חיים חדש ב-DB: `PLANNING_PENDING_APPROVAL`.
    2.  ה-Orchestrator יפיק עץ משימות (Task Tree).
    3.  כאשר משתמש מאשר את התוכנית ב-UI/API, האישור יישמר יחד עם מזהה גרסה (Version ID) או גיבוב (Hash) בלתי-ניתן-לשינוי של התוכנית המקורית, ועם מזהה המשתמש המאשר.
    4.  בעת מעבר ל-Execution, המערכת תאמת ותפעיל בדיוק את גרסת ה-Plan שאושרה. תוכניות שעודכנו ידרשו אישור חדש ולא ניתן יהיה לבצע Execution על סמך אישור ישן לתוכנית שהשתנתה.

### 4.3 זיכרון לטווח ארוך (Long-Term Memory / Cursor Rules)

*   **הקשר:** המערכת צריכה ללמוד חוקים ספציפיים (כמו Cursor Rules), אך ללא סיכוני אבטחה של זליגת מידע או הרעלת פרומפטים.
*   **תתי-שלבים:**
    1.  הקמת טבלת `project_rules` עם בידוד מלא לפי Repository (Tenant Isolation) ומדיניות Retention/Deletion מוגדרת.
    2.  משימת-רקע (Post-task execution): מודל קטן יפיק חוקים מהשיחה, אך יבצע Redaction קפדני ל-PII ולסודות, וישמור מידע לגבי מקור החוק (Provenance).
    3.  חוקים לא יישמרו אוטומטית, אלא ידרשו אישור מפורש של המשתמש.
    4.  כאשר ה-Orchestrator ישלוף חוקים למשימה חדשה, הם יסומנו כקלט לא-מהימן (Untrusted Input) ויעברו סינון, במקום להיות מוזרקים כהוראות מהימנות ל-System Prompt.

---

## שלב 5: בדיקות חזותיות אקטיביות (Visual Web Testing)

**המטרה:** סגירת מעגל הפיתוח הפרונט-אנדי האוטונומי. בדומה ל-Devin או v0, הסוכן יבחן ויזואלית את ממשק המשתמש שהוא עצמו יצר ויוודא שהוא עומד בדרישות.

### 5.1 שילוב דפדפן Headless בסביבת ה-Sandbox

*   **הקשר:** הסוכן זקוק לדפדפן כדי לראות באמת כיצד הדף נראה לאחר ששונה.
*   **תתי-שלבים:**
    1.  הטמעת ספריית Playwright או Puppeteer בהגדרות מערכת ההפעלה של ה-Vercel Sandbox.
    2.  פיתוח סקריפט-עזר שיודע להרים שרת פיתוח מקומי (למשל `npm run dev`) בארגז החול, וממתין בצורה חכמה עד שהשרת פתוח להאזנה (למשל על פורט 3000).

### 5.2 צילום מסך, העברה למודל ראייה ותיקון עצמי

*   **הקשר:** הפלטפורמה תספק ל-Agent "עיניים" כדי שיוכל להעביר ביקורת עצמית (Self-Critique).
*   **תתי-שלבים:**
    1.  בסיום כתיבת קוד ה-UI, ה-Agent יפעיל Tool ייעודי שקורא ל-Headless Browser לנווט לנתיב שנוצר/השתנה באפליקציה (למשל `http://localhost:3000/dashboard`).
    2.  המערכת תיקח צילום מסך (Screenshot) ואף תחלץ את ה-DOM הסופי במידת הצורך.
    3.  המערכת תעביר את צילום המסך למודל התומך בראייה חזותית (Vision Model, כגון GPT-4o, Claude 3.5 Sonnet, או Kimi) יחד עם שאלות הבוחנות את עמידתו בדרישות המקוריות.
    4.  במידה והמודל מזהה פער ויזואלי (לדוגמה, "כפתור ההרשמה דורס את הטקסט שמעליו" או "הצבע אינו תואם"), הביקורת מוזנת חזרה אל ה-Agent Loop. הסוכן מבין את השגיאה החזותית, מתקן את הקוד (Visual Self-Healing), ומצלם שוב עד לאישור סופי.

---

## סיכום אופרטיבי והנחיות ביצוע

מסמך זה פורט לרזולוציה העמוקה ביותר את כל המרכיבים ההכרחיים כדי לבנות פלטפורמת פיתוח אוטונומית מתקדמת.
מומלץ לנהל את העבודה לפי ה-Epics הבאים, בסדר המתחשב בתלויות הטכניות (Stack Dependencies):
1. **Epic 1:** שדרוג ל-Smart Model Router (שלב 1) - מהווה בסיס (Baseline) חיוני לניהול נכון של הבקשות והעלויות לפני שמפעילים סוכנים אינטנסיביים. (✅ בוצע)
2. **Epic 2:** חשיפת ה-API Router (שלב 2) - מבסס את התשתית להתממשקות חיצונית ומאפשר לבחון את המערכת ללא תלות בממשק משתמש מלא, לפני העמקת הלוגיקה של הסוכן.
3. **Epic 3:** פיתוח Native Cloud Agent והכלים המותאמים אישית (שלב 3) - נבנה על גבי תשתית הניתוב וה-API ליצירת הלוגיקה העצמאית של הסוכן (ללא הראוטרים חלק מהכלים המותאמים אישית לא יוכלו לרוץ ביעילות ובעלויות סבירות).
4. **Epic 4:** בניית ה-Orchestrator (שלב 4) - הרחבה מתקדמת שנענית על גבי יכולות הסוכן העצמאי והראוטרים הקיימים.
5. **Epic 5:** בדיקות חזותיות אקטיביות (שלב 5) - שלב סופי לסגירת Edge-cases של UI, המסתמך על תקינות כל השלבים הקודמים.
